# Quest — guide agents

## Layout

- `server/` — FastAPI, port **8001**, env `QUEST_*`
- `mobile_app/` — Android
- Public : `https://quest.instree.org`

## Commandes

```bash
./server.sh
./server.sh --local
./release-github.sh
./release-github.sh lan
./run-cable.sh
./run-cable.sh --build
```
