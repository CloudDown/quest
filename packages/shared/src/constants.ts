/** Rayon max (mètres) pour soumettre un check-in sur place */
export const CHECK_IN_RADIUS_METERS = 100;

/** Délai avant expiration d'un check-in non validé (heures) */
export const CHECK_IN_VALIDATION_TIMEOUT_HOURS = 12;

/** Heures de diffusion des notifs quest */
export const NOTIFICATION_WINDOW = { start: 7, end: 21 } as const;
